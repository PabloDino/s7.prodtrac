<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="@raw/selfsigned2"/>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>

    <!-- for this to be active, line below needs to be included in manifest
        android:networkSecurityConfig="@xml/network_security_config"
        early on I had used this in development until i got useful https resources
      -->